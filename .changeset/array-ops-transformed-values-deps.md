---
"mobx-formly": minor
---

Submit schema- and resolver-transformed values through `handleSubmit`, reconcile dirty fields and validate the complete form after `mutate`, warm up `isValid` with a schema-driven validation pass, and support `deps` plus named `validate` record in register options
